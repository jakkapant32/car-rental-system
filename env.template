# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=car_rental
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Configuration
# Generate secure secrets using: openssl rand -base64 32
# Or PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-characters-change-in-production
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
# For production with multiple frontends (comma-separated):
# FRONTEND_URLS=https://customer.example.com,https://admin.example.com,https://staff.example.com

# Stripe Payment Gateway (Optional - leave empty for development)
# Get your keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Email Configuration (Optional - for sending booking confirmations)
# For Gmail: Enable 2FA and use App Password from https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@carrental.com

# File Upload Configuration
UPLOAD_DIR=./uploads
QR_STORAGE_PATH=./uploads/qr-codes


